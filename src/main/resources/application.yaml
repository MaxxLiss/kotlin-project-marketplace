spring:
  application:
    name: kotlin-project-marketplace

  datasource:
    url: jdbc:postgresql://localhost:5432/kotlin_project_marketplace_db
    name: postgres
    password: password

server:
  port: 8082